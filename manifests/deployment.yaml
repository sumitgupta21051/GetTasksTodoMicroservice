apiVersion: apps/v1
kind: Deployment
metadata:
  name: get-tasks
spec:
  replicas: 3
  selector:
    matchLabels:
      app: get-tasks
  template:
    metadata:
      labels:
        app: get-tasks
    spec:
      containers:
      - name: get-tasks
        env:
          - name: CONNECTION_STRING
            value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:sqlserver-aks.database.windows.net,1433;Database=databse-sql1;Uid=adminuser;Pwd=thisIsKat11;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"                                       
        image: acrrg1.azurecr.io/gettasktodo:v2
        ports:
        - containerPort: 8000
